contract_version: 2
feature: "homepage-auth-session"
contract_status: "superseded"
superseded_by:
  - "docs/adr/0006-frontend-pkce-backend-jwt-bearer-boundary.md"
endpoint:
  method: "GET"
  path: "/api/auth/oidc/callback"
  owner: "Architecture"

request:
  path_params: []
  query_params: []
  headers: []

responses:
  success:
    status: 404
    behavioral_guarantees:
      - "This endpoint is out of scope and MUST NOT be implemented by backend in current scope."
      - "Authorization code callback handling is frontend OIDC client responsibility."
    schema:
      type: object
      required: ["code", "message"]
      properties:
        code: { type: string }
        message: { type: string }
    example:
      code: "not_found"
      message: "Endpoint not found"

  errors:
    - status: 409
      code: "contract_violation"
      schema:
        type: object
        required: ["code", "message", "details"]
        properties:
          code: { type: string }
          message: { type: string }
          details:
            type: array
            items: { type: string }
      example:
        code: "contract_violation"
        message: "Attempted implementation of superseded backend callback endpoint"
        details:
          - "Use frontend OIDC client callback handling"

frontend_contract_notes:
  query_key: "none"
  cache_policy: "Frontend callback processing updates auth state and route navigation without backend callback transport endpoint."
  route_usage: "OIDC redirect/callback route in frontend"

compatibility_rules:
  additive_only: true
  breaking_change_process: "requires ADR update"
  supersession_rules:
    - "Any reintroduction of this endpoint requires a new ADR and a new endpoint contract"

